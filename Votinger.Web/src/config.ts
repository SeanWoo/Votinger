export const GATEWAY_URL = "https://localhost:5000";
export const API_VERSION = "api";

export const AUTH_SERVER_URL = `${GATEWAY_URL}/${API_VERSION}/Auth`;
export const POLL_SERVER_URL = `${GATEWAY_URL}/${API_VERSION}/Poll`;
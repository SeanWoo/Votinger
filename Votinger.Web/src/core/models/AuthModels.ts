export type SignInModel = {
    login: string;
    password: string;
}
export type SignUpModel = {
    login: string;
    password: string;
}
export type TokensModel = {
    accessToken: string,
    refreshToken: string
}
